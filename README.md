Twitter Trends Storm Topology
------------------------------
Yet another storm topology to read twitter streams. Yawn!

Configuration
==============

Place a `config.properties` file under `src/main/resources/` with the following properties:

```
KafkaTopic={YOUR-TOPIC}
```
